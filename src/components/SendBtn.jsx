export function SendBtn ({text}) {

    return(
        <button 
        type="submit"
        className="ml-auto bg-moderate-blue text-white px-8 py-3 rounded-lg text-center hover:opacity-50 md:snd-md md:px-0">
            {text}</button>
    )
}